package Models;

public class HumanPlayer extends Player {

    public HumanPlayer(String id, String name, Symbol symbol, PlayerType playerType) {
        super(id, name, symbol, playerType);
    }
}
